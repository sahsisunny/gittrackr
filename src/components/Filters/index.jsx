import './filter.css';

const Filters = ({ children }) => {
     return (
               <div className="checkbox">
                    {children}
               </div>
     );
};

export default Filters;
